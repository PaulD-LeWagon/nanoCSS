<% content_for :title, "Component Catalogue - nanoCSS" %>
<% content_for :head do %>
  <% if @css.present? %>
    <style id="nanocss-catalogue-styles">
      <%= @css.html_safe %>
    </style>
  <% end %>
<% end %>

<div class="catalogue-container">
  <header class="catalogue-header">
    <h1>Component Catalogue</h1>
    <p>A visual reference and code repository for all <%= @prefix %> components.</p>
  </header>

  <style>
    .catalogue-container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 4rem 2rem;
    }
    .catalogue-header {
      margin-bottom: 3rem;
      text-align: center;
    }
    .catalogue-header h1 {
      font-size: clamp(2.5rem, 5vw, 3.5rem);
      font-weight: 800;
      margin-bottom: 1rem;
      background: linear-gradient(135deg, var(--chrome-text), var(--chrome-muted));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      letter-spacing: -0.03em;
    }
    .catalogue-header p {
      font-size: 1.25rem;
      color: var(--chrome-muted);
      max-width: 60ch;
      margin: 0 auto;
    }
    .component-section { 
      background: var(--chrome-glass); 
      backdrop-filter: blur(12px);
      border: 1px solid var(--chrome-border);
      border-radius: 1rem; 
      margin-bottom: 3rem; 
      overflow: hidden; 
      color: var(--chrome-text);
      box-shadow: 0 4px 24px -12px rgba(0,0,0,0.5);
    }
    .component-header { 
      padding: 1.5rem 2rem; 
      border-bottom: 1px solid var(--chrome-border); 
      display: flex; 
      justify-content: space-between; 
      align-items: center; 
      background: rgba(255,255,255,0.03);
    }
    .component-header h2 { 
      margin: 0; 
      font-size: 1.25rem; 
      font-weight: 600; 
      font-family: sans-serif; 
    }
    .component-preview { 
      padding: 2rem; 
      background: #f8fafc; /* Keep light background so nanoCSS styles are visible */
      color: #111827;
    }
    .component-code { 
      background: #0f172a; 
      color: #f3f4f6; 
      padding: 1.5rem; 
      position: relative; 
      border-top: 1px solid var(--chrome-border);
    }
    .component-code pre { 
      margin: 0; 
      overflow-x: auto; 
      font-family: monospace; 
      font-size: 0.875rem; 
    }
    .copy-btn { 
      position: absolute; 
      top: 1rem; 
      right: 1rem; 
      background: var(--chrome-border); 
      color: white; 
      border: none; 
      padding: 0.5rem 1rem; 
      border-radius: 4px; 
      cursor: pointer; 
      transition: background 0.2s; 
    }
    .copy-btn:hover {
      background: var(--chrome-muted);
    }
    /* UC-039: Sidebar layout */
    .catalogue-layout {
      display: grid;
      grid-template-columns: 200px 1fr;
      gap: 2.5rem;
      align-items: start;
    }
    .catalogue-main {
      min-width: 0; /* prevent grid cell from overflowing due to pre/code min-content */
    }
    .catalogue-sidebar {
      position: sticky;
      top: 5rem;
      max-height: calc(100vh - 6rem);
      overflow-y: auto;
      scrollbar-width: thin;
    }
    .catalogue-sidebar h3 {
      font-size: 0.7rem;
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--chrome-muted);
      margin: 0 0 0.75rem;
      padding: 0 0.5rem;
    }
    .catalogue-sidebar nav ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    .catalogue-sidebar nav ul li a {
      display: block;
      padding: 0.35rem 0.6rem;
      border-radius: 0.375rem;
      font-size: 0.8rem;
      text-decoration: none;
      color: var(--chrome-muted, #9ca3af);
      transition: background-color 0.15s, color 0.15s;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .catalogue-sidebar nav ul li a:hover {
      background: rgba(255,255,255,0.08);
      color: var(--chrome-text, #f9fafb);
    }
    .catalogue-sidebar nav ul li a.active {
      background: rgba(99,102,241,0.18);
      color: var(--chrome-text, #f9fafb);
      font-weight: 600;
    }
    @media (max-width: 768px) {
      .catalogue-layout {
        grid-template-columns: 1fr;
      }
      .catalogue-sidebar {
        display: none;
      }
    }
  </style>

  <div class="catalogue-layout" data-controller="catalogue-sidebar">
    <aside class="catalogue-sidebar">
      <h3>Components</h3>
      <nav aria-label="Component navigation">
        <ul>
          <li><a href="#text-banner" data-catalogue-sidebar-target="navLink" data-section-id="text-banner">Text Banner</a></li>
          <li><a href="#hero-banner" data-catalogue-sidebar-target="navLink" data-section-id="hero-banner">Hero Banner</a></li>
          <li><a href="#carousel" data-catalogue-sidebar-target="navLink" data-section-id="carousel">Carousel</a></li>
          <li><a href="#accordion" data-catalogue-sidebar-target="navLink" data-section-id="accordion">Accordion</a></li>
          <li><a href="#card" data-catalogue-sidebar-target="navLink" data-section-id="card">Card</a></li>
          <li><a href="#card-variants" data-catalogue-sidebar-target="navLink" data-section-id="card-variants">Card Variants</a></li>
          <li><a href="#dropdown" data-catalogue-sidebar-target="navLink" data-section-id="dropdown">Dropdown</a></li>
          <li><a href="#group" data-catalogue-sidebar-target="navLink" data-section-id="group">Group</a></li>
          <li><a href="#loading" data-catalogue-sidebar-target="navLink" data-section-id="loading">Loading</a></li>
          <li><a href="#modal" data-catalogue-sidebar-target="navLink" data-section-id="modal">Modal</a></li>
          <li><a href="#navbar" data-catalogue-sidebar-target="navLink" data-section-id="navbar">Nav / NavBar</a></li>
          <li><a href="#progress" data-catalogue-sidebar-target="navLink" data-section-id="progress">Progress</a></li>
          <li><a href="#tooltip" data-catalogue-sidebar-target="navLink" data-section-id="tooltip">Tooltip</a></li>
          <li><a href="#buttons" data-catalogue-sidebar-target="navLink" data-section-id="buttons">Buttons</a></li>
          <li><a href="#badges" data-catalogue-sidebar-target="navLink" data-section-id="badges">Badges</a></li>
          <li><a href="#tags" data-catalogue-sidebar-target="navLink" data-section-id="tags">Tags</a></li>
          <li><a href="#breadcrumbs" data-catalogue-sidebar-target="navLink" data-section-id="breadcrumbs">Breadcrumbs</a></li>
          <li><a href="#pagination" data-catalogue-sidebar-target="navLink" data-section-id="pagination">Pagination</a></li>
          <li><a href="#tabs" data-catalogue-sidebar-target="navLink" data-section-id="tabs">Tabs</a></li>
          <li><a href="#icons" data-catalogue-sidebar-target="navLink" data-section-id="icons">Icons</a></li>
        </ul>
      </nav>
    </aside>

    <div class="catalogue-main">

    <!-- 0. Text Banner -->
    <section id="text-banner" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Text Banner</h2></div>
      <div class="component-preview">
        <hgroup>
          <h1>Main Headline Here</h1>
          <p>This is the supporting subtitle text directly underneath.</p>
        </hgroup>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;hgroup&gt;
  &lt;h1&gt;Main Headline Here&lt;/h1&gt;
  &lt;p&gt;This is the supporting subtitle text directly underneath.&lt;/p&gt;
&lt;/hgroup&gt;</code></pre>
      </div>
    </section>

    <!-- 1. Hero Banner -->
    <section id="hero-banner" class="component-section" data-controller="clipboard">

      <div class="component-header">
        <h2>The Hero Banner</h2>
      </div>      
      <div class="component-preview">
        
        <section class="<%= @prefix %>-hero">
          <h1>Big Hero Headline</h1>
          <p>Supporting value prop goes here, expanding upon the headline.</p>
          <button class="<%= @prefix %>-btn-primary">Get Started</button>
        </section>

      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>

        <pre><code data-clipboard-target="source">&lt;section class="<%= @prefix %>-hero"&gt;
  &lt;h1&gt;Big Hero Headline&lt;/h1&gt;
  &lt;p&gt;Supporting value prop goes here, expanding upon the headline.&lt;/p&gt;
  &lt;button class="<%= @prefix %>-btn-primary"&gt;Get Started&lt;/button&gt;
&lt;/section&gt;</code></pre>
      </div>
    </section>

    <!-- 2. Carousel -->
    <section id="carousel" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Carousel</h2></div>
      <div class="component-preview" style="background: #1e293b; padding: 0; border-radius: 0.5rem; overflow: hidden;">
        <div class="<%= @prefix %>-carousel" id="demo-carousel" style="border-radius: 0;">
          <div class="carousel-track" id="demo-carousel-track">
            <div class="carousel-slide" style="height: 200px; background: linear-gradient(135deg,#6366f1,#a855f7); display:flex; align-items:center; justify-content:center; color:#fff; font-size:1.5rem; font-weight:700;">Slide 1</div>
            <div class="carousel-slide" style="height: 200px; background: linear-gradient(135deg,#ec4899,#f59e0b); display:flex; align-items:center; justify-content:center; color:#fff; font-size:1.5rem; font-weight:700;">Slide 2</div>
            <div class="carousel-slide" style="height: 200px; background: linear-gradient(135deg,#10b981,#0ea5e9); display:flex; align-items:center; justify-content:center; color:#fff; font-size:1.5rem; font-weight:700;">Slide 3</div>
          </div>
          <button class="carousel-btn carousel-prev" onclick="carouselScroll('demo-carousel-track', -1)" aria-label="Previous slide">‹</button>
          <button class="carousel-btn carousel-next" onclick="carouselScroll('demo-carousel-track',  1)" aria-label="Next slide">›</button>
          <ul class="carousel-dots" id="demo-carousel-dots">
            <li class="active"></li><li></li><li></li>
          </ul>
        </div>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;div class="<%= @prefix %>-carousel"&gt;
  &lt;div class="carousel-track"&gt;
    &lt;div class="carousel-slide"&gt;Slide 1&lt;/div&gt;
    &lt;div class="carousel-slide"&gt;Slide 2&lt;/div&gt;
    &lt;div class="carousel-slide"&gt;Slide 3&lt;/div&gt;
  &lt;/div&gt;
  &lt;button class="carousel-btn carousel-prev" onclick="..."›‹&lt;/button&gt;
  &lt;button class="carousel-btn carousel-next" onclick="..."&gt;›&lt;/button&gt;
  &lt;ul class="carousel-dots"&gt;&lt;li class="active"&gt;&lt;/li&gt;...&lt;/ul&gt;
&lt;/div&gt;</code></pre>
      </div>
    </section>

    <script>
      function carouselScroll(trackId, direction) {
        const track = document.getElementById(trackId);
        const slideWidth = track.offsetWidth;
        track.scrollBy({ left: direction * slideWidth, behavior: 'smooth' });
        // Update dots after scroll settles
        setTimeout(() => {
          const index = Math.round(track.scrollLeft / slideWidth);
          const carousel = track.closest('[id$="-carousel"]') || track.closest('.' + track.id.replace('-track', ''));
          const dotsId = trackId.replace('-track', '-dots');
          const dots = document.querySelectorAll('#' + dotsId + ' li');
          dots.forEach((d, i) => d.classList.toggle('active', i === index));
        }, 350);
      }
    </script>

    <!-- 3. Accordion (UC-060: first-class .{prefix}-accordion wrapper) -->
    <section id="accordion" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Accordion</h2></div>
      <div class="component-preview">
        <%# Exclusive mode: shared name attribute → one open at a time (browser-native, zero JS) %>
        <div class="<%= @prefix %>-accordion">
          <details name="catalogue-demo" open>
            <summary>Exclusive Panel 1</summary>
            <p>Content for panel one. Only one panel can be open at a time (native browser mutual exclusivity via <code>name</code>).</p>
          </details>
          <details name="catalogue-demo">
            <summary>Exclusive Panel 2</summary>
            <p>Content for panel two. Clicking here closes panel one automatically.</p>
          </details>
        </div>

        <%# Independent mode: no name attribute → each panel toggles independently %>
        <div class="<%= @prefix %>-accordion <%= @prefix %>-accordion--independent" style="margin-top: 1rem;">
          <details>
            <summary>Independent Panel A</summary>
            <p>This panel toggles independently. Multiple panels can be open at the same time.</p>
          </details>
          <details>
            <summary>Independent Panel B</summary>
            <p>Omit the <code>name</code> attribute to allow multiple panels open simultaneously.</p>
          </details>
        </div>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;!-- Exclusive mode (one open at a time) --&gt;
&lt;div class="<%= @prefix %>-accordion"&gt;
  &lt;details name="my-accordion"&gt;
    &lt;summary&gt;Panel Title&lt;/summary&gt;
    &lt;p&gt;Panel content&lt;/p&gt;
  &lt;/details&gt;
  &lt;details name="my-accordion"&gt;
    &lt;summary&gt;Panel Title 2&lt;/summary&gt;
    &lt;p&gt;Panel content&lt;/p&gt;
  &lt;/details&gt;
&lt;/div&gt;

&lt;!-- Independent mode (any open at a time) --&gt;
&lt;div class="<%= @prefix %>-accordion <%= @prefix %>-accordion--independent"&gt;
  &lt;details&gt;
    &lt;summary&gt;Panel A&lt;/summary&gt;
    &lt;p&gt;Panel content&lt;/p&gt;
  &lt;/details&gt;
&lt;/div&gt;</code></pre>
      </div>
    </section>

    <!-- 4. Card -->
    <section id="card" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Card</h2></div>
      <div class="component-preview">
        <article class="<%= @prefix %>-card" style="max-width: 400px;">
          <header>Card Title</header>
          <div class="card-body">This is the main body of the card, populated with standard readable text.</div>
          <footer><button class="<%= @prefix %>-btn-primary">Action</button></footer>
        </article>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;article class="<%= @prefix %>-card"&gt;
  &lt;header&gt;Card Title&lt;/header&gt;
  &lt;div class="card-body"&gt;Content...&lt;/div&gt;
  &lt;footer&gt;&lt;button class="<%= @prefix %>-btn-primary"&gt;Action&lt;/button&gt;&lt;/footer&gt;
&lt;/article&gt;</code></pre>
      </div>
    </section>

    <!-- 4a-d. Card Variants -->
    <section id="card-variants" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Card Variants</h2></div>
      <div class="component-preview" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1rem;">

        <article class="<%= @prefix %>-card-primary">
          <header>Primary</header>
          <div class="card-body">Header uses <code>--<%= @prefix %>-primary</code>.</div>
          <footer><button class="<%= @prefix %>-btn-primary">Action</button></footer>
        </article>

        <article class="<%= @prefix %>-card-secondary">
          <header>Secondary</header>
          <div class="card-body">Header uses <code>--<%= @prefix %>-secondary</code>.</div>
          <footer><button class="<%= @prefix %>-btn-secondary">Action</button></footer>
        </article>

        <article class="<%= @prefix %>-card-tertiary">
          <header>Tertiary</header>
          <div class="card-body">Header uses <code>--<%= @prefix %>-tertiary</code>.</div>
          <footer><button class="<%= @prefix %>-btn-outline">Action</button></footer>
        </article>

        <article class="<%= @prefix %>-card-neutral">
          <header>Neutral</header>
          <div class="card-body">Header uses <code>--<%= @prefix %>-neutral-700</code> from the neutral shade scale.</div>
          <footer>Info only</footer>
        </article>

      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;!-- Primary --&gt;
&lt;article class="<%= @prefix %>-card-primary"&gt;...&lt;/article&gt;

&lt;!-- Secondary --&gt;
&lt;article class="<%= @prefix %>-card-secondary"&gt;...&lt;/article&gt;

&lt;!-- Tertiary --&gt;
&lt;article class="<%= @prefix %>-card-tertiary"&gt;...&lt;/article&gt;

&lt;!-- Neutral --&gt;
&lt;article class="<%= @prefix %>-card-neutral"&gt;...&lt;/article&gt;</code></pre>
      </div>
    </section>

    <!-- 5. Dropdown -->
    <section id="dropdown" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Dropdown</h2></div>
      <div class="component-preview" style="height: 200px;">
        <div class="<%= @prefix %>-dropdown" data-controller="dropdown">
          <button class="<%= @prefix %>-btn-outline" data-action="click->dropdown#toggle">Actions ▼</button>
          <ul>
            <li>Edit Profile</li>
            <li>Settings</li>
             <li>Logout</li>
          </ul>
        </div>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;div class="<%= @prefix %>-dropdown" data-controller="dropdown"&gt;
  &lt;button class="<%= @prefix %>-btn-outline" data-action="click-&gt;dropdown#toggle"&gt;Actions ▼&lt;/button&gt;
  &lt;ul&gt;
    &lt;li&gt;Edit Profile&lt;/li&gt;
    &lt;li&gt;Settings&lt;/li&gt;
  &lt;/ul&gt;
&lt;/div&gt;</code></pre>
      </div>
    </section>

    <!-- 6. Group -->
    <section id="group" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Group</h2></div>
      <div class="component-preview">
        <div class="<%= @prefix %>-group">
          <input type="text" placeholder="Search..." style="border: 1px solid #d1d5db; padding: 0.5rem;" />
          <button class="<%= @prefix %>-btn-primary">Submit</button>
        </div>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;div class="<%= @prefix %>-group"&gt;
  &lt;input type="text" placeholder="Search..." /&gt;
  &lt;button class="<%= @prefix %>-btn-primary"&gt;Submit&lt;/button&gt;
&lt;/div&gt;</code></pre>
      </div>
    </section>

    <!-- 7. Loading -->
    <section id="loading" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Loading</h2></div>
      <div class="component-preview">
        <span class="<%= @prefix %>-loader"></span>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;span class="<%= @prefix %>-loader"&gt;&lt;/span&gt;</code></pre>
      </div>
    </section>

    <!-- 8. Modal -->
    <section id="modal" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Modal</h2></div>
      <div class="component-preview">
        <button class="<%= @prefix %>-btn-outline" onclick="document.getElementById('demo-modal').showModal()">Open Modal</button>
        <dialog id="demo-modal" class="<%= @prefix %>-modal">
          <h2 style="margin-top: 0;">Modal Title</h2>
          <p>Please confirm your action to proceed.</p>
          <div style="display: flex; gap: 1rem; justify-content: flex-end; margin-top: 1.5rem;">
            <button class="<%= @prefix %>-btn-outline" onclick="document.getElementById('demo-modal').close()">Cancel</button>
            <button class="<%= @prefix %>-btn-primary" onclick="document.getElementById('demo-modal').close()">Confirm</button>
          </div>
        </dialog>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;dialog class="<%= @prefix %>-modal" id="my-modal"&gt;
  &lt;h2&gt;Modal Title&lt;/h2&gt;
  &lt;p&gt;Modal content goes here.&lt;/p&gt;
  &lt;button onclick="document.getElementById('my-modal').close()"&gt;Close&lt;/button&gt;
&lt;/dialog&gt;</code></pre>
      </div>
    </section>

    <!-- 9. Nav / NavBar -->
    <section id="navbar" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Nav / NavBar</h2></div>
      <div class="component-preview">
        <nav class="<%= @prefix %>-nav">
          <div style="font-weight: bold; font-size: 1.25rem;">BrandLogo</div>
          <ul class="nav-menu">
            <li><a href="#">Home</a></li>
            <li><a href="#">About</a></li>
            <li><a href="#">Contact</a></li>
          </ul>
        </nav>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;nav class="<%= @prefix %>-nav"&gt;
  &lt;div&gt;BrandLogo&lt;/div&gt;
  &lt;ul class="nav-menu"&gt;
    &lt;li&gt;&lt;a href="#"&gt;Home&lt;/a&gt;&lt;/li&gt;
    &lt;li&gt;&lt;a href="#"&gt;About&lt;/a&gt;&lt;/li&gt;
  &lt;/ul&gt;
&lt;/nav&gt;</code></pre>
      </div>
    </section>

    <!-- 10. Progress -->
    <section id="progress" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Progress</h2></div>
      <div class="component-preview">
        <progress value="45" max="100">45%</progress>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;progress value="45" max="100"&gt;45%&lt;/progress&gt;</code></pre>
      </div>
    </section>

    <!-- 11. Tooltip -->
    <section id="tooltip" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Tooltip</h2></div>
      <div class="component-preview">
        <span class="<%= @prefix %>-tooltip" tabindex="0" data-tooltip="This is an informational message!">Hover or Focus Me</span>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;span class="<%= @prefix %>-tooltip" data-tooltip="Informational message here"&gt;Hover Me&lt;/span&gt;</code></pre>
      </div>
    </section>

    <!-- 12. Buttons -->
    <section id="buttons" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Buttons</h2></div>
      <div class="component-preview" style="display: flex; gap: 1rem; align-items: center;">
        <button class="<%= @prefix %>-btn-primary">Primary</button>
        <button class="<%= @prefix %>-btn-secondary">Secondary</button>
        <button class="<%= @prefix %>-btn-outline">Outline</button>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;button class="<%= @prefix %>-btn-primary"&gt;Primary&lt;/button&gt;
&lt;button class="<%= @prefix %>-btn-secondary"&gt;Secondary&lt;/button&gt;
&lt;button class="<%= @prefix %>-btn-outline"&gt;Outline&lt;/button&gt;</code></pre>
      </div>
    </section>

    <!-- 13. Badges -->
    <section id="badges" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Badges</h2></div>
      <div class="component-preview" style="display: flex; gap: 0.5rem;">
        <span class="<%= @prefix %>-badge">New</span>
        <span class="<%= @prefix %>-badge success">Approved</span>
        <span class="<%= @prefix %>-badge warning">Pending</span>
        <span class="<%= @prefix %>-badge danger">Error</span>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;span class="<%= @prefix %>-badge"&gt;New&lt;/span&gt;
&lt;span class="<%= @prefix %>-badge success"&gt;Approved&lt;/span&gt;</code></pre>
      </div>
    </section>

    <!-- 14. Tags -->
    <section id="tags" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Tags</h2></div>
      <div class="component-preview" style="display: flex; gap: 0.5rem;">
        <span class="<%= @prefix %>-tag">Ruby <button type="button" aria-label="Remove">×</button></span>
        <span class="<%= @prefix %>-tag">Rails <button type="button" aria-label="Remove">×</button></span>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;span class="<%= @prefix %>-tag"&gt;
  Ruby &lt;button type="button" aria-label="Remove"&gt;×&lt;/button&gt;
&lt;/span&gt;</code></pre>
      </div>
    </section>

    <!-- 15. Breadcrumbs -->
    <section id="breadcrumbs" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Breadcrumbs</h2></div>
      <div class="component-preview">
        <nav class="<%= @prefix %>-breadcrumb" aria-label="breadcrumb">
          <ol>
            <li><a href="#">Home</a></li>
            <li><a href="#">Products</a></li>
            <li>Shoes</li>
          </ol>
        </nav>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;nav class="<%= @prefix %>-breadcrumb" aria-label="breadcrumb"&gt;
  &lt;ol&gt;
    &lt;li&gt;&lt;a href="#"&gt;Home&lt;/a&gt;&lt;/li&gt;
    &lt;li&gt;&lt;a href="#"&gt;Products&lt;/a&gt;&lt;/li&gt;
    &lt;li&gt;Shoes&lt;/li&gt;
  &lt;/ol&gt;
&lt;/nav&gt;</code></pre>
      </div>
    </section>

    <!-- 16. Pagination -->
    <section id="pagination" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Pagination</h2></div>
      <div class="component-preview">
        <ul class="<%= @prefix %>-pagination">
          <li class="disabled"><span>&laquo;</span></li>
          <li class="active"><span>1</span></li>
          <li><a href="#">2</a></li>
          <li><a href="#">3</a></li>
          <li><a href="#">&raquo;</a></li>
        </ul>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;ul class="<%= @prefix %>-pagination"&gt;
  &lt;li class="disabled"&gt;&lt;span&gt;&amp;laquo;&lt;/span&gt;&lt;/li&gt;
  &lt;li class="active"&gt;&lt;span&gt;1&lt;/span&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href="#"&gt;2&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href="#"&gt;&amp;raquo;&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;</code></pre>
      </div>
    </section>

    <!-- 17. Tabs -->
    <section id="tabs" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Tabs</h2></div>
      <div class="component-preview">
        <div class="<%= @prefix %>-tabs">
          <input type="radio" id="tab1" name="demo_tabs" checked />
          <label for="tab1">Overview</label>
          <div class="panel" id="panel1">This is the overview panel content. Zero JS.</div>

          <input type="radio" id="tab2" name="demo_tabs" />
          <label for="tab2">Specifications</label>
          <div class="panel" id="panel2">These are the technical specifications. Built natively.</div>
          
          <input type="radio" id="tab3" name="demo_tabs" />
          <label for="tab3">Reviews</label>
          <div class="panel" id="panel3">Customer reviews go here. Completely semantic.</div>
        </div>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;div class="<%= @prefix %>-tabs"&gt;
  &lt;input type="radio" id="tab1" name="tabs" checked /&gt;
  &lt;label for="tab1"&gt;Tab 1&lt;/label&gt;
  &lt;div class="panel" id="panel1"&gt;Panel 1 Content&lt;/div&gt;

  &lt;input type="radio" id="tab2" name="tabs" /&gt;
  &lt;label for="tab2"&gt;Tab 2&lt;/label&gt;
  &lt;div class="panel" id="panel2"&gt;Panel 2 Content&lt;/div&gt;
&lt;/div&gt;</code></pre>
      </div>
    </section>

    <!-- 18. Icons -->
    <section id="icons" class="component-section" data-controller="clipboard">
      <div class="component-header"><h2>Icons</h2></div>
      <div class="component-preview" style="display: grid; grid-template-columns: repeat(auto-fill, minmax(120px, 1fr)); gap: 1.5rem; text-align: center;">
        <% ["menu", "close", "chevron-up", "chevron-down", "chevron-left", "chevron-right", "check", "alert", "info", "search", "user", "home", "settings", "star", "heart", "download", "copy", "link", "external-link", "moon", "sun"].each do |icon| %>
          <div class="icon-item" style="padding: 1rem; border-radius: 0.5rem; border: 1px solid #e5e7eb; background: #fff;">
            <div style="margin-bottom: 0.5rem; color: var(--<%= @prefix %>-primary);"><%= nano_icon(icon, size: 32) %></div>
            <div style="font-size: 0.75rem; font-family: monospace; color: #6b7280;"><%= icon %></div>
          </div>
        <% end %>
      </div>
      <div class="component-code">
        <button class="copy-btn" data-action="clipboard#copy">Copy</button>
        <pre><code data-clipboard-target="source">&lt;%= nano_icon("menu", size: 24, class: "custom-class") %&gt;</code></pre>
      </div>
    </section>

    </div><%# .catalogue-main %>
  </div><%# .catalogue-layout %>
</div><%# .catalogue-container %>
